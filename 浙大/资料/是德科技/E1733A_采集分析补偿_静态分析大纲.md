# E1733A 采集 / 分析 / 补偿 — 静态分析大纲（全面深入版）

> **用途**：只学「采集 / 分析 / 补偿」三块核心时，按阶段把提问贴进 Cursor。  
> **「不遗漏」** = 清单每一行都有 `status`，不是还原整份厂商软件。  
> **工作区（本机）**：`G:\浙大\资料\是德科技\` 或解包目录 `extracted/`  
> **安装包源头**：`https://github.com/Montyzhang/-seed` → `Install Keysight E1733A 1.14.1 (Win64).exe`  
> **学习包（建议自建）**：`e1733a_learning/`（账本 JSON、coverage、测试）  
> **冻结源头（目标形态）**：`.static-analysis/`（JSON / 哈希 / RVA / 指令窗）— **当前多数项尚未冻结，先 candidate/unknown**

---

## 0. 每次开新对话先贴（总则）

```
只做冻结静态分析。源头是工作区 .static-analysis 的 JSON/哈希/RVA/指令窗；
在冻结完成前，可暂用：解包文件清单、E1733A_Remote.h、Sample.*、English.csv、DLL 导出表。
不加载/执行厂商 PE，不联网/串口/设备，不写 CNC 补偿文件/厂商数据文件，不下发 NC。
学习代码（E4 预检、八态机、自写复现）不能反向证明厂商实现。
无新指令窗就保持 unknown，禁止把旁证（调用点、Remote.h 常量、跳表、字符串表）升级成公式或状态机。
输出必须是目录表：identifier / 模块 / RVA / 窗哈希 / status / boundary / 缺什么。
空单元格 = 遗漏；不要用「其余不重要」跳过未列表项。
对照已有 catalog_e1733a_acq / catalog_e1733a_ana / catalog_e1733a_cmp / catalog_formats、
static_catalog、coverage.json，只补空行，不要重做已闭合项。
回复用简体中文。
```

---

## 1. 三块分别「问完即验收」

### 1.1 采集（不漏）

| 原 xd_laser 概念 | E1733A 对应（待钉死） | 当前静态证据 |
|------------------|----------------------|--------------|
| 九类菜单 | `E1733A_CC_NEW(40)` 下 11+ 测量类型 | Remote.h E1 |
| 采集文档/视图 | Delphi 类名未知（`CCollectDoc` 等价物） | **unknown** |
| Start / Trigger / Reset | `CC_START(53)` / `TrigType` Manual·Encoder·Auto / `CC_RESET(52)` | Remote.h + Sample.Lin E1 |
| 硬件任务层 | **E1735A.dll**（非 LMS） | 导出表 E1 |
| 任务生命周期 | `StartExternalSampling` / `StopExternalSampling` / `SetupTimer` / `StartTimer` / `StopTimer` | 导出表 candidate |
| 环境同步读 | `E1736A_ReadEnvironment` / `ReadAllSensors` | 导出表 candidate |
| 原始数据处理 | **`E1735ACore_ProcessRawData`** | 仅符号 E1，**函数体 unknown** |
| 未知/仅边界 | `ReadRegisters` / `WriteRegisters` / `GetParameter` / `SetParameter` | candidate |
| GUI→硬件桥 | `E1733A.exe` → `E1735A.dll` | import 表存在，**proven_bridge=null**（无 RVA 链） |

**采集验收清单（每行须有 status）：**

```
ACQ-E1-MEATYPE-LINEAR      … NEW 命令 CI_LINEAR=0
ACQ-E1-MEATYPE-ANGULAR     … CI_ANGULAR=1
ACQ-E1-MEATYPE-STR         … CI_STRAIGHTNESS=2
ACQ-E1-MEATYPE-SQU         … CI_SQUARENESS=3
ACQ-E1-MEATYPE-PAR         … CI_PARALLELISM=4
ACQ-E1-MEATYPE-ROT         … CI_ROTARY=5
ACQ-E1-MEATYPE-WAY         … CI_WAYSTRAIGHTNESS=6
ACQ-E1-MEATYPE-FLA         … CI_FLATNESS=7
ACQ-E1-MEATYPE-DIA         … CI_DIAGONAL=8
ACQ-E1-MEATYPE-LTB         … CI_LINTIMEBASE=9
ACQ-E1-MEATYPE-ATB         … CI_ANGTIMEBASE=10
ACQ-E1-MEATYPE-STB         … CI_STRTIMEBASE=11
ACQ-E1-MEATYPE-DUAL        … CI_DUALAXIS=14
ACQ-E1-CMD-START           … CC_START=53
ACQ-E1-CMD-RECORD          … CC_RECORD=54
ACQ-E1-CMD-RESET           … CC_RESET=52
ACQ-E1-CMD-STOP            … CC_STOP=55
ACQ-E1-TRIG-MANUAL         … TrigType=0
ACQ-E1-TRIG-ENCODER        … TrigType=1 + AQBMode/AQBRes
ACQ-E1-TRIG-AUTO           … TrigType=2 + TrigWnd/TrigDwell
ACQ-E1-DLL-ReadSample      … E1735A.dll 导出
ACQ-E1-DLL-ProcessRawData  … E1735ACore.dll 导出，body unknown
ACQ-E1-DLL-ReadEnvironment … E1736A.dll 导出，body unknown
ACQ-UNK-DELPHI-COLLECTDOC  … 采集文档类，无 RTTI
ACQ-UNK-PAUSE-RESUME       … 导出表无 pause/resume 等价项 → keep unknown
```

### 1.2 分析（最容易漏，优先补）

| 原 xd_laser 概念 | E1733A 对应 | 当前证据 |
|------------------|-------------|----------|
| 离线数据分析入口 | `CC_SETUP` → `CI_ANALYSIS=5`；菜单字符串 `Data Analysis` | Remote.h + English.csv |
| 结果文档/视图 | Delphi 类名未知（无 `CLinDoc` 等符号） | **unknown** |
| 有样本扩展名 | `.Lin` `.Ang` `.str` `.Squ` `.Par` `.rot` `.Way` `.Fla` `.Dia` `.LTB` `.ATB` `.STB` `.LDA` | Sample.* E1 |
| 统计输出 API | `CC_ANALYSIS(66)` + `CI_BIACCURACY` 等 30+ 常量 | Remote.h E1 |
| 标准选择 | `ANASETUP_STANDARD` 0–12（ISO 230-2 多版本等） | Remote.h E1 |
| 图形/数值开关 | `ANASETUP_ANAGRAPH` / `ANASETUP_ANANUM` Comprise 位串 | Remote.h + Sample.Lin |
| 算法叶（类似 ALG-08） | ISO 230-2 统计、Moody 平面度、VDI 专用量 | **函数体 unknown** |
| 桥：打开文件→算法 | Option Description File → 内部类 | **proven_bridge=null** |

**分析验收清单：**

```
ANA-E1-ENTRY-SETUP-ANALYSIS    … CI_ANALYSIS=5
ANA-E1-ENTRY-OPEN-DATA         … CC_OPEN SETUPDATA=1
ANA-E1-API-READERRORDATA       … CC=62
ANA-E1-API-ANALYSIS-ACCURACY   … CC=66, CI_BIACCURACY=11
ANA-E1-API-ANALYSIS-REPEAT     … CI_BIREPEATPOS=8
ANA-E1-API-ANALYSIS-REVERSAL   … CI_BIMAXREV=0, CI_BIMEANREV=1
ANA-E1-API-ANALYSIS-SYSDEV     … CI_BISYSPOSDEV=5
ANA-E1-API-ANALYSIS-SLOPE-LS   … CI_SLOPELS=19
ANA-E1-API-ANALYSIS-SLOPE-EP   … CI_SLOPEEP=20
ANA-E1-STD-ISO230-2006         … STANDARD=11
ANA-E1-STD-ISO230-2014         … STANDARD=12
ANA-E1-STD-GB10931            … STANDARD=8
ANA-E1-GRAPH-BACKLASH         … AnaGraph 位「Backlash/pt.」
ANA-E1-NUM-6SIGMA             … AnaNum 位 8
ANA-E1-FLA-MOODY              … FlaMethod=0
ANA-UNK-DELPHI-LINDOC         … 无 PDB/RTTI
ANA-UNK-DELPHI-STRDOC         … unknown
ANA-UNK-ALG-ISO230-BODY       … 无指令窗
ANA-UNK-ALG-VDI-BODY          … 仅 API 常量，无公式窗
```

### 1.3 补偿（不漏）

| 原 xd_laser 概念 | E1733A 对应 | 当前证据 |
|------------------|-------------|----------|
| 环境补偿函数体 | `E1736A_ReadEnvironment` + `ENVSETUP_*` | API/配置 E1，**body unknown** |
| 测距/波长补偿 | `E1735ACore_ProcessRawData` + `SYSSETUP_WAVELENGTH` | **body unknown** |
| 补偿表 UI/逻辑 | `CMPSETUP_*` + `CC_SHOWCOMPENSATION(32)` | Remote.h E1 |
| 补偿导出 | `CC_SAVE` → `COMPTABLE_TXT/CSV/POS/RUN` | Remote.h E1 |
| CNC 下发 | `CompProg` / 55291A（包外） | **forbidden_writer** |
| 文件格式 | Option Description File 内 `Comp*` 字段（非独立 `.cmp`） | Sample.Lin E1 |

**补偿验收清单：**

```
CMP-E1-UI-SHOWCOMP            … CC_SHOWCOMPENSATION=32
CMP-E1-CFG-SIGN-CORRECTION    … CompSign=0
CMP-E1-CFG-ABS-INCREMENTAL    … CompCalc Absolute/Incremental
CMP-E1-CFG-DIR-COMBINED       … CompDir Combined/Fwd-Rev
CMP-E1-CFG-SEL-TARGETLIST     … CompSel TargetList/Interpolate
CMP-E1-CFG-GRID-START-END     … CompStart/End/Intvl
CMP-E1-EXPORT-COMPTABLE-CSV   … CC_SAVE COMPTABLE_CSV=6
CMP-E1-SAMPLE-LINEARERR       … Sample.Lin LinearErr 字段
CMP-UNK-AMBIENT-BODY          … E1736A 内部公式
CMP-UNK-LASERDIST-BODY        … ProcessRawData 内部公式
CMP-UNK-INTERPOLATE-ALG       … CompSel=Interpolate 插值实现
CMP-FORBID-CNC-WRITER         … 禁止写 Fanuc/NC
CMP-FORBID-55291A-BINARY      … 包外工具，仅登记 boundary
```

### 1.4 必须保持 `null` 的桥（任何阶段不得画成一条链）

| # | 禁止连接的桥 | 原因 |
|---|-------------|------|
| 1 | `.LTB` / 时基文档 → 速度公式 `Velocity_Calculation` | 无速度算法窗；仅 `CI_VEL_*` 分析 API |
| 2 | `E1736A_ReadEnvironment` 函数体 → `ProcessRawData` 同路径 | 模块分离，无冻结调用链 |
| 3 | `CompSel=Interpolate` → 样条/三次插值 | 无指令窗，禁止从 Remote.h 升级 |
| 4 | `E1733A.exe` 采集 UI → `E1735A` 任务实例连续性 | import 存在 ≠ 实例桥 |
| 5 | 自写 ISO230 复现 / E4 预检 → 厂商采集状态机 | 学习代码非厂商证据 |
| 6 | `LinearErr` 样本字段 → 内部 `CC_ANALYSIS` 实现 | 数据存在 ≠ 算法等价 |
| 7 | `Wavelength Compensation` 字符串 → Edlén/Ciddor 公式选型 | 仅 UI 术语 |

---

## 2. 阶段怎么用

按顺序做。每阶段只复制「本阶段提问」。做完用「阶段 7 收口」贴一次。

| 阶段 | 做什么 | 完成标准 |
|------|--------|----------|
| **0** | 建账本骨架 + 解包清单哈希 | `coverage.json` 有三块区段；53 文件各有 SHA256 |
| **A** | 分析侧：标准/API/扩展名/未知 Delphi 类 | 每个分析 identifier/扩展名一行，无公式 |
| **B** | 采集/补偿：DLL 导出与未知函数体 | 无新窗不升级；`ProcessRawData` 保持 unknown |
| **C** | 三块两两桥矩阵 | 每格只有 `proven_bridge` 或 `null` |
| **D** | 格式胶水：Option Description File ↔ 测量类型 | parser / unsupported / forbidden_writer |
| **停止** | 无新窗、无新类、无新扩展名 | 宣布「目录完整」，非「软件掌握」 |

**当前进度建议**：阶段 0 部分完成（解包+Remote.h+Sample）；**下一优先 = 阶段 A**。

---

## 3. 阶段 0 — 冻结基础设施（先于 A，若尚未建 `.static-analysis`）

```
【总则】见第 0 节。

【本阶段只做 0】为 E1733A 建立与 xd_laser 同形态的冻结层，不分析算法。

必须产出：
1. extracted/ 下 53 个文件的 SHA256 清单 → manifest_files.json
2. E1735A.dll / E1735ACore.dll / E1736A.dll / E1733A.exe 的 PE 导出/导入 → pe_exports.json
3. E1733A_Remote.h 解析为 setup_constants.json（全部 E1733A_CI_* / CC_*）
4. 每个 Sample.* 的 MeaType + 扩展名映射 → sample_manifest.json
5. coverage.json 空矩阵（acq/ana/cmp 各 identifier 先标 missing）

禁止：反汇编函数体、写公式、跑 PE。
每行：identifier, 模块, source_path, file_sha256, status, boundary, 缺什么。
```

---

## 4. 阶段 A — 分析目录（下一优先，直接复制）

```
【总则】只做冻结静态分析。源头是 .static-analysis 或 Remote.h/Sample.*/English.csv。
不加载 PE，不设备 I/O，不写 CNC 补偿文件。
E4 学习代码不能证明厂商。无新窗保持 unknown。回复简体中文。

【本阶段只做 A】分析侧静态目录，做成与 ACQ-E1-MEATYPE-* 同级的账本行。
不要写公式，不要做采集状态机，不要重复已闭合的 Remote.h 常量行。

必须枚举并登记：
1. 离线/在线数据分析入口：
   - CC_SETUP → CI_ANALYSIS=5
   - CC_SHOWGRAPH(30) / CC_SHOWRAWDATATABLE(31) / CC_SHOWCOMPENSATION(32)
   - English.csv 中 "Data Analysis" / "Numerical Analysis" / "Recalculate"
2. 结果侧（Delphi 类名未知，一律 ANA-UNK-DELPHI-*）：
   按测量类型登记逻辑文档槽位：
   Lin, Ang, Str, Squ, Par, Rot, Way, Fla, Dia, LTB, ATB, STB, LDA
   （不要求恢复 CLinDoc 等真实类名，除非 PE 冻结出 RTTI）
3. 每个结果扩展名一行（有样本标 sample_scope_parser，无样本标 unsupported）：
   有样本：.Lin .Ang .str .Squ .Par .rot .Way .Fla .Dia .LTB .ATB .STB .LDA
   导出子格式：RAWDATA_CSV/POS/RUN（Remote.h SAVE 子类型）
4. CC_ANALYSIS(66) 全部 CI 常量各一行（30+ 项）
5. ANASETUP_STANDARD 0–12 各一行（标准枚举）
6. ANASETUP_ANANUM / ANAGRAPH Comprise 每一位一行（位含义见 Remote.h 注释）
7. 已有 ANA-E1-* 不要重复造 ID；缺的才 NEW。

每行字段：identifier, 模块, RVA 或 source_identifier, 窗/descriptor 哈希,
status（E1 仅当有冻结 descriptor 或指令窗哈希，否则 candidate/unknown）,
boundary, 缺什么。

先输出「空行矩阵」，再只补空行。
对照 catalog_e1733a_ana / coverage.json 的 ANA 区段。
```

---

## 5. 阶段 B — 未知导出不升级（直接复制）

```
【总则】只读 .static-analysis 或 pe_exports.json。不加载 PE 执行。
无新完整函数窗不得把 unknown 升为 E1。旁证只能进 attachments。回复简体中文。

【本阶段只做 B】复核采集/补偿侧 catalog-only 导出与边界。

E1735A.dll 导出（至少）：
ReadSample, ReadSampleAndAQB, ReadAllSamples, ReadSampleCount,
SetupAQB, ReadAQB, SetSampleTriggers, GetSampleTriggers,
StartExternalSampling, StopExternalSampling,
SetupTimer, StartTimer, StopTimer, ReadTimerSamples,
ReadBeamStrength, GetOptics, SetOptics, GetParameter, SetParameter

E1735ACore.dll 导出（至少）：
ProcessRawData, ReadRawData, ReadRegisters, WriteRegisters,
SearchCards, ConfigureCard

E1736A.dll 导出（至少）：
ReadEnvironment, ReadSensor, ReadAllSensors, GetSensorInfo

只扫 JSON 顶层键（若已冻结）：
exports, pe_imports, remote_h_constants, sample_field_map,
ambient_evidence, laser_distance_evidence, compensation_ui_evidence

已有旁证必须保持：
- ProcessRawData: export_symbol_only（不是完整测距公式）
- ReadEnvironment: export_symbol_only（不是 Edlén/Ciddor 选型证明）
- CompSel Interpolate: remote_h_constant_only（不是插值公式）

输出：每个短名的 rva / has_complete_formula_window / attachments / status。
有新的 body_range.sha256 或完整函数窗才标 window_candidate；否则 keep_unknown。
```

---

## 6. 阶段 C — 三块桥矩阵（直接复制）

```
【总则】只做静态桥表。不得发明状态机。不得把学习复现当厂商证据。回复简体中文。

【本阶段只做 C】输出 采集 × 分析 × 补偿 两两桥矩阵（3×3 子表 + 关键文件桥）。

行/列用已有 identifier 或阶段 A/B 目录短名。
每个格子只允许：
- proven_bridge=<证据一句话 + 路径/哈希>
- proven_bridge=null
禁止「可能」「应该会接到」。

下列格子必须为 null（若写成有桥，指出冲突并改回 null）：
1. FMT-LTB / 时基样本 → ALG-VELOCITY / 速度公式
2. CMP-UNK-AMBIENT-BODY → CMP-UNK-LASERDIST-BODY 同路径或同调用
3. CompSel Interpolate → 样条/三次插值求值
4. ACQ-E1-CMD-START → E1735A 任务实例在 E1733A.exe 内的连续性
5. 自写 ISO230 脚本 / E4 → 厂商 Armed/Running/三种触发语义

另列「仍空的桥」：
- 打开 Sample.Lin → CC_ANALYSIS 返回值（无动态测试则 null）
- LinearErr 字段 → CompCalc 输出（无指令窗则 null）
- E1735A_ReadSample → ProcessRawData 调用链（无 CFG 则 null）

不要实现任何公式。不要改 FORBID 项。
```

---

## 7. 阶段 D — 格式胶水（直接复制）

```
【总则】只读 sample_manifest.json 与 Option Description File 文本样本。
不执行 E1733A.exe 写文件。不写 CNC/55291A 输出。回复简体中文。

【本阶段只做 D】把分析/补偿用到的扩展名接到「逻辑文档槽位」，不接到算法。

对每个扩展名一行：
disposition（sample_scope_parser / unsupported / forbidden_writer）、
绑定的逻辑槽位 Lin|Ang|…（仅当 Sample 或 Remote.h 有证据）、
是否有冻结样本、status。

必须包括：
有样本 parser：Lin Ang str Squ Par rot Way Fla Dia LTB ATB STB LDA
SAVE 子类型：RAWDATA_TXT CSV POS RUN；COMPTABLE_TXT CSV POS RUN；ENVDATA_TXT CSV
forbidden_writer：任何 CNC 下发、55291A 二进制、厂商覆盖写 Sample.*
unsupported（当前无样本）：若安装包内不存在则标 missing_sample

Option Description File 规则：
- 头尾标记 <Option Description File - Rev. 1.00a>
- Identify=Key,Type=Value 行
- Comprise/TextUnit/ItemText/Choosing 语法
- cnf 不适用；本软件无 5891.cnf

VLC/LTB 行必须写 proven_bridge=null 到速度公式。
对照 catalog_formats 与阶段 A 分析表。
```

---

## 8. 阶段收口（每阶段做完贴一次）

```
对照本阶段清单：哪些行仍空？只许回答空行列表（identifier + 缺的字段）。
有空行：只补目录，不许开始写公式或状态机。
无空行：列出本阶段新增 identifier，以及仍为 unknown 的导出（确认没有新窗）。

若已建测试，跑：
pytest tests/test_e1733a_catalog_phase0.py
tests/test_e1733a_unknown_exports.py
tests/test_e1733a_evidence_drift.py
在 e1733a_learning/ 下执行。失败就修，不要扩 scope。
```

---

## 9. 停止条件（直接复制）

```
请检查是否同时满足停止条件（只回答是/否 + 反例）：
1. 采集 13 类测量 + 触发三态 + DLL 导出，分析 CC_ANALYSIS/标准/扩展名，补偿 CMPSETUP/导出，目录无空行。
2. 无新的 body_range.sha256 / 完整函数窗仍标为 unknown。
3. 阶段 C 的 7 条强制 null 桥仍为 null。
4. CNC writer / 55291A 下发 / 覆盖写厂商样本 仍为 forbidden。
5. 没有自写八态机冒充厂商采集状态机。
若全是，结论：三块核心「目录完整」；不是厂商软件等价，不是掌握运行行为。
```

---

## 10. 10 个子代理并行（整段复制）

```
开 10 个子代理并行，文件互斥，禁止多人改 static_catalog.py。

只读（不写文件）：
1. 扫 E1733A_Remote.h：全部 CC/CI 常量 → 分析/采集/补偿分类
2. 扫 pe_exports.json：E1735A/E1735ACore/E1736A 导出
3. 扫 sample_manifest + 13 个 Sample.*：MeaType 与字段名
4. 扫 English.csv：Standards/Compensation/Environmental 字符串 ID
5. 对照 coverage.json 标空行 + 检查 7 条强制 null 桥

只写（各改各的）：
6. catalog_e1733a_acq.py — 只加采集目录行
7. catalog_e1733a_ana.py — 只加分析目录行
8. catalog_formats.py — Option Description File + 扩展名 disposition
9. tests/test_e1733a_analysis_catalog.py — 只测 ANA 行完整性
10. catalog_e1733a_cmp.py — window_audit 差异，不升级 CMP-UNK-*

父代理：装配 EvidenceLedger、跑阶段 8 回归。
安全：不加载 PE，不设备 I/O，不写 CNC。回复简体中文。
```

---

## 11. 心理预期（收藏用，不用贴给 Cursor）

### 按本大纲做完，你能得到：

- **采集 / 分析 / 补偿** 的完整地图：每条在哪、证据到哪、桥断在哪
- **能学习的叶子**：
  - Remote.h 全部配置项与 `CC_ANALYSIS` 输出枚举
  - `Sample.Lin` 的 `LinearErr` + ISO 230-2 **标准侧**手算对照
  - `E1735A` 导出 API 边界（采什么）
  - `CMPSETUP` 补偿表参数语义（输出什么格式）

### 得不到、也不要再问到「掌握」的：

- `ProcessRawData` 内 Edlén 还是 Ciddor、具体系数
- 三种触发在固件层的精确状态机
- 打开 `Sample.Squ` 就自动等价于厂商验收图
- 补偿写入 Fanuc/Siemens 控制器
- 55291A 专有格式与 RS-232 协议
- 真机激光采集与 NC 运动

### 附录（非核心课）：

- `KSE173xA.sys` USB 驱动 IOCTL
- `Setup.exe` 安装流程
- 多语言 `*.csv` 翻译
- `E1733A_Automation.pdf` 演示程序（动态层，非冻结静态）

---

## 12. 与 xd_laser 原模板的逐项对照

| xd_laser 原项 | E1733A 映射 | 备注 |
|---------------|-------------|------|
| `CCollectDoc` / `CCol*View` | 无符号；用 `ACQ-E1-MEATYPE-*` + `ACQ-UNK-DELPHI-*` | Delphi 无 PDB |
| `XWinnerLMS.dll` | `E1735A.dll` + `E1735ACore.dll` | 非 LMS 命名 |
| `createSimTask` 等 | `StartExternalSampling` / `SetupTimer` | 语义近似，**非等价** |
| `CAnaChildFrame` / `CLinDoc` | `ANA-UNK-DELPHI-*` + 扩展名槽位 | |
| `.sqr/.flt/.vlc` | `.Squ/.Fla/.LTB` 等 | 扩展名不同 |
| `ALG-08` 五片窗 | `ANA-UNK-ALG-ISO230-BODY` 等 | 需后续反汇编冻结 |
| `CalculateAmbientEnv` | `E1736A_ReadEnvironment` | 仅 API |
| `Calculate_LaserDistance +0x7A8` | `E1735ACore_ProcessRawData` | 无 RVA 叶，全 unknown |
| `.cmp/.prm` | Option Description File 内字段 | 非独立扩展名 |
| `catalog_xdlaser` | `catalog_e1733a_*` | 建议自建 |

---

## 13. 推荐目录结构（学习包）

```
e1733a_learning/
├── .static-analysis/          # 后续冻结：disasm JSON、RVA、指令窗哈希
├── manifests/
│   ├── manifest_files.json    # 53 文件 SHA256
│   ├── pe_exports.json
│   ├── setup_constants.json   # 从 Remote.h 解析
│   └── sample_manifest.json
├── catalogs/
│   ├── catalog_e1733a_acq.py
│   ├── catalog_e1733a_ana.py
│   ├── catalog_e1733a_cmp.py
│   └── catalog_formats.py
├── coverage.json
├── EvidenceLedger.json
└── tests/
    ├── test_e1733a_catalog_phase0.py
    └── test_e1733a_unknown_exports.py
```

---

*文档版本：v1.0 | 对应安装包 E1733A 1.14.1 (Win64) | 方法论源自 xd_laser 冻结静态分析流程*
