# 执行状态

| 项 | 状态 |
|----|------|
| 骨架 | 已建立 |
| 固件入库 | `firmware/niusbFirebolt.cfg` + `niusbFireboltFPGA.cfg` |
| 照片入库 | 未默认提交；见 `manifests/photo_index.json` 远程索引 |
| 静态冻结 | **未冻结**（骨架迭代中） |
| 抓包 | 不做（本阶段） |

生成账本：`make verify`
