# G2 当日执行包（测点 → JSON → 命题回填）

> 在 Day-1 完成后使用。产出可直接喂给 inbox ingest。  
> **最短清单**：`G2_投放三步.md`。  
> 探点：`G2_照片探针地图.md`（飞线优先 R83/R88/R89/R105/R106；拨位基线 SW5=OFF/OFF）。

---

## 1. 时钟 JSON

复制 `g2_inbox/g2_clocks.template.json` → 填实测 Hz → 存为 `g2_inbox/g2_clocks.json`（根目录）。

最少：C2 或 C3 一条非 null。

---

## 2. SPI CSV

LA 导出放到 `g2_inbox/spi_capture.csv`。

```bash
# 单独译码（可选；列名可用 Saleae 别名）
python3 scripts/decode_spi_capture.py capture.csv --auto-map --json g2_spi_decode.json
python3 scripts/g2_mode_infer.py --clocks g2_clocks.json --spi g2_spi_decode.json
```

推荐一键：

```bash
# 文件已在 05_tests/g2_inbox/ 根目录后
python3 scripts/ingest_g2_inbox.py
```

人工复核 `P1.3/P1.4/P1.5` 建议后写入：

- `05_tests/G2_时钟与SPI记录.md`
- `06_docs/G0_命题基线证据表.md`（改等级须附报告内 sha256）

---

## 3. 最小合格（G2 Gate）

- [ ] C2 或 C3 至少一条测得并落入 122.88/245.76/491.52 族 → 冲 P1.3 ✅  
- [ ] checklist 出现 A2（0x41 LVDS）或 D1（CONFIG1）或等价强靶标 → 冲 P1.4 ✅  
- [ ] 「仅上电」SPI 有/无 记入 P1.5  

未达标不得开 G3 算法定性。
