# 位流特征与软核线索

## 扫描方法
对 `20230825_s2056.mcs` 展开镜像（6,692,588 B）做 ASCII / 关键字搜索。

## 结果

| 探测项 | 结果 | 等级 |
|--------|------|------|
| MicroBlaze / PicoBlaze 明文 | **未找到** | 🔶 倾向无软核明文特征 |
| lwIP / AXI / TEMAC / FTDI 明文 | **未找到** | 🔶 |
| 任意 ≥6 字符相关 printable 串 | **0** | ✅ 扫描复现 |
| 同步字 / IDCODE | 仍为明文配置头 | ✅ |

## 解释
MCS/裸位流通常**不含** Vivado 工程字符串；无明文 ≠ 无软核。  
当前仅能结论：**未发现可直接证明 MicroBlaze/lwIP 的字符串证据**。

## 对通信架构的含义
- 以太网更可能是 **纯逻辑 MAC + 自定义 UDP**（🔶），而非 lwIP 软栈  
- USB 侧同理：硬件 FIFO 桥接为主（与 FT600 匹配）  
- Q20「是否有软核」保持 **❓**，需 ILA/行为或专业位流工具再判

## 复现
```bash
python3 - <<'PY'
# 见本仓库 scripts；关键字扫描 MicroBlaze/lwIP/AXI/FTDI 等
PY
```
