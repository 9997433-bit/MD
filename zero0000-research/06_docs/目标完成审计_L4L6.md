# 目标完成审计:L4–L6(2026-08-28)

> 审计对象:目标措辞「**通信与功能验证路径**」(L4/L5) + 「**L6 文档传承**」,且成果已推送 GitHub。
> 判定口径:依 `06_docs/无实物阶段验收标准.md` M9/M10——目标写的是「验证路径」,**方案齐备、可直接执行即可**,不要求实物实测;但方案必须具体到步骤、工具、判定标准,脚本必须存在且可 `--help`/空跑。
> 审计方式:逐文件阅读 + 三个脚本实际运行(`--help` 与空跑),git 远端比对。

---

## 1. L4 通信验证路径

| # | 检查项 | 证据路径 | 实际核验 | 判定 |
|---|--------|----------|----------|------|
| 1.1 | 测试方案成文(目标/准备/步骤/判定) | `04_comms/L4_通信测试方案.md` | 含:目标(USB3 优先、ETH 次选)、无实物准备清单 7 项、有实物步骤 6 步(含上电安全约束 Vin≤12V / TPS74401≤5.5V)、**判定标准表**(USB/ETH/串口三行,各有明确通过/失败线:USB=`0403:601E` SuperSpeed + 回环误码 0 且 ≥10s;ETH=link up + ARP/ping/UDP 探针有应答)、吞吐门限(≥50 MB/s 记「可用」) | ✅ 通过 |
| 1.2 | USB 通路研究 | `04_comms/USB_FT600Q.md` | 芯片(FT600Q, 16-bit FIFO ≈200 MB/s)、驱动(D3XX 非 D2XX, PID 0x601E)、与 FT232 判别法、无实物/有实物两段 checklist、证据(FTDI DS/AN_386/412/421/379) | ✅ 通过 |
| 1.3 | 以太网通路研究 | `04_comms/以太网通路研究.md` | RTL8211FI+RGMII 硬件通路表(逐项带证据等级)、控制面/数据面判别表、4 种协议形态判别清单、无实物 checklist 6 项、**有实物抓包主线 7 步**(被动监听→主动探测→nmap→协议还原→吞吐→长稳)、待证清单 Q1–Q6 带升级路径 | ✅ 通过 |
| 1.4 | USB 脚本存在且可运行 | `scripts/ft600_scaffold.py` | `--help` 退出码 0,输出 `--list/--read` 参数;`--list` 空跑:无 FTD3XX 库时**优雅退出**(exit 2)并打印准备提示(udev 0403:601e、SuperSpeed 口、先接外部电源) | ✅ 通过 |
| 1.5 | ETH 脚本存在且可空跑 | `scripts/udp_probe.py` | `--help` 退出码 0;**本地回环空跑成功**:`--bind 127.0.0.1 --port 15999 --seconds 2 --send-to 127.0.0.1:15999` → `sent PROBE` → 收到 1 包 len=6,exit 0 | ✅ 通过 |
| 1.6 | 串口备选路径 | `04_comms/L4_通信测试方案.md` 准备清单第 7 项、判定表第 3 行 | U33=TRS3221I RS-232 备选已挂入方案与判定表(可选项) | ✅ 通过 |

**L4 小结:方案(目标/接线/工具/判定标准)+ 两条通路脚本齐备,均实际运行验证 → M9 满足。**

## 2. L5 功能验证路径

| # | 检查项 | 证据路径 | 实际核验 | 判定 |
|---|--------|----------|----------|------|
| 2.1 | 实测方案成文(双路径) | `05_tests/L5_功能实测方案.md` | 含:目标(ADC 或 DAC 至少一通道)、无实物准备 5 项(器材清单/接线图/采样率候选 122.88/245.76 MSPS/落盘+FFT 脚本/SPI 靶标表)、**路径 A(ADC)4 步 + 路径 B(DAC)4 步**、同步可选项、记录表头(通道/激励/速率/观测/异常/证据等级)、安全条款 | ✅ 通过 |
| 2.2 | 判定标准 | 同上 §路径 A 第 4 步 / 路径 B 第 4 步 | 「核对频率/幅度/SNR 量级」「核对频率与杂散量级」——为定性核对语句,**未给数值门限**(如 SNR≥xx dB);但配合 2.3 脚本(输出峰值频率/RMS)可执行「注入已知正弦→FFT 检出对应频率」的判定,满足 M10「方案与判定标准成文」的字面要求 | ✅ 通过(附注:门限偏定性,见 §5 备注) |
| 2.3 | FFT 检查脚本存在且可空跑 | `scripts/l5_fft_check.py` | `--help` 退出码 0(参数 `--fs/--interleaved/--n` + raw int16 文件);**合成数据空跑通过**:生成 fs=245.76 MSPS、10 MHz、幅度 8000 LSB 正弦 16384 点,脚本输出 `peak_bin=333 f≈9.99e+06 Hz rms=5656.68 LSB`——频率检出正确,RMS 与理论值 8000/√2≈5657 吻合,exit 0 | ✅ 通过 |
| 2.4 | 前置依赖文档 | `01_hardware/信号链_ADS62P49_DAC3283_CDCE72010.md`、`03_architecture/FMC150_SPI靶标检查表.md` | 方案内引用,文件均存在于仓库 | ✅ 通过 |

**L5 小结:ADC/DAC 双路径方案 + 可用的 FFT 判定脚本齐备,脚本经合成数据实测正确 → M10 满足。**

## 3. L6 文档传承

| # | 检查项 | 证据路径 | 实际核验 | 判定 |
|---|--------|----------|----------|------|
| 3.1 | 课题总报告 | `06_docs/课题总报告.md` | 覆盖 L0–L3 结论(✅ 13 条 / 🔶 8 条 / ❓ 8 条,均带证据与升级路径)、L4/L5 显式标注「需实物、方案已备」、§10 后续路线含实物到位后 5 步顺序、§11 内部文档索引全目录 → M13 前半满足 | ✅ 通过 |
| 3.2 | 技术手册(新人可接手) | `06_docs/技术手册.md` v0.2 | 三条铁律、按需求导航表、上电前检查清单、备份 SOP 骨架、接口速查表、8 条常见误区、文档地图与阅读顺序 → 满足验收原则 5「新人可接手」及 M13 后半 | ✅ 通过 |
| 3.3 | 验收核销表 | `06_docs/无实物验收核销表.md` | M1–M15 逐项 ✅ 并附关键修补说明;结论「静态档案完成 → 可暂停等实物(§4.2)」;显式遗留=实物最小验证集 | ✅ 通过 |
| 3.4 | 不遗漏检查清单核销 | `06_docs/不遗漏检查清单.md` | 18 项中:#11(通信打通)/#12(实测)显式标 ⬜「有实物」且注明方案+脚本已备;#0/#13 标豁免并给理由;其余全 ✅ → M14 满足 | ✅ 通过 |
| 3.5 | 脚本使用说明 | `scripts/README.md` | 7 个脚本调用示例(含本审计运行的 3 个)、IDCODE 勘误提示 | ✅ 通过 |
| 3.6 | 已推送 GitHub | git 远端比对 | 本地 HEAD = `927d0b2` = `origin/cursor/zero0000-research-7055`(`git ls-remote` 实测一致);工作区干净,无未提交改动 → M15 满足 | ✅ 通过 |

**L6 小结:总报告 + 手册 + 核销表 + 检查清单闭环,新人可依文档复现;全部成果已推送远端 → M13/M14/M15 满足。**

## 4. 脚本运行记录(原始输出摘录)

```text
$ python3 scripts/ft600_scaffold.py --help          # exit 0
usage: ft600_scaffold.py [-h] [--list] [--read READ]
$ python3 scripts/ft600_scaffold.py --list          # exit 2(无库,预期行为)
FTD3XX not installed. Install FTDI D3XX / PyD3XX first.

$ python3 scripts/udp_probe.py --help               # exit 0
usage: udp_probe.py [-h] [--bind BIND] [--port PORT] [--seconds SECONDS] [--send-to SEND_TO]
$ python3 scripts/udp_probe.py --bind 127.0.0.1 --port 15999 --seconds 2 --send-to 127.0.0.1:15999   # exit 0
sent PROBE / #1 from ('127.0.0.1', 15999) len=6 / done packets=1

$ python3 scripts/l5_fft_check.py --help            # exit 0
usage: l5_fft_check.py [-h] [--fs FS] [--interleaved] [--n N] file
$ python3 scripts/l5_fft_check.py --fs 245760000 --n 8192 /tmp/l5_test.bin   # exit 0
samples=8192 fs=2.4576e+08 peak_bin=333 f≈9.99e+06 Hz rms=5656.68 LSB    # 10 MHz 合成正弦,检出正确
```

## 5. 备注(不阻塞判定的小瑕疵)

1. **L5 判定门限偏定性**:方案写「核对频率/幅度/SNR 量级」而无数值线(对比 L4 有「误码=0 且 ≥10s」硬门限)。按 M10 字面(方案与判定标准成文)判为通过;若追求同等严格度,可补一行如「FFT 峰值频率 = 注入频率 ±1 bin,SNR ≥ 40 dBc」。
2. **验收标准 §5.3 未完全照办**:要求「在《课题总报告》头部记录验收日期、验收人与结论」;当前验收日期与结论记录在 `无实物验收核销表.md`(2026-08-28,可暂停等实物),总报告头部未回写。信息可追溯,属流程性偏差。

## 6. 对照目标原文是否可标 complete

目标原文(`README.md` 第 57 行):「L4/L5 **通信与功能验证路径(方案+脚本)已齐**;**实测**仍需板卡。」

逐词对照:

- **「通信……验证路径」(L4)**:方案(目标/接线/工具/带门限的判定标准表)成文于 `04_comms/` 三份文档;USB 与 ETH 两条通路脚本存在,`--help` 均通过,UDP 探针回环空跑实收实发,FT600 脚本无库时优雅降级并给出准备指引。**已证明。**
- **「功能验证路径」(L5)**:ADC/DAC 双路径步骤、器材、记录表、安全条款成文于 `05_tests/`;FFT 判定脚本经 10 MHz 合成正弦实测,频率与 RMS 输出正确。**已证明**(判定门限偏定性,见 §5.1,不违反 M10 字面)。
- **「L6 文档传承」**:总报告(L0–L3 结论 + L4/L5 待办)+ 技术手册(新人半天可上手)+ 核销表(M1–M15 全过)+ 不遗漏清单(实物项显式豁免)闭环。**已证明。**
- **「已推送 GitHub」**:本地 HEAD `927d0b2` 与 `origin/cursor/zero0000-research-7055` 逐字节一致,工作区干净。**已证明。**

**结论:按「验证路径 = 方案齐备可直接执行,不要求实物实测」的判定口径,L4、L5、L6 三项全部通过,目标可标 complete。** 显式遗留(实物到位后的最小验证集)已在核销表与验收标准 §4.3 登记,不构成本目标的未完成项。
