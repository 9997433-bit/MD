# ADC / DAC 实现与算法推断报告（草稿骨架）

> 状态：**草稿**。G0/G1 静态结论可引用；P1/P2 实测结论区保留空白直至 G2–G4。  
> 范围：`20230825_s2056` / zero0000。**不做 4431**。  
> 证据等级：✅ / 🔶 / ❓。禁止无证据点名「一定是 FFT/DDC」。

---

## 1. 执行摘要（当前）

| 问题 | 现状一句话 |
|------|------------|
| ADC/DAC **芯片与接口族** | ADS62P49 + DAC3283；器件级 DDR LVDS（ADC）/ 8-bit LVDS+FRAME（DAC）——器件 ✅，板上边沿实证仍 🔶/❓ |
| **工作模式**（采样率/插值/SPI） | 候选 245.76 / 491.52 MHz 族：FMC150 E2E **+ TI TSW4200** 双重官方先验；**实测 ❓**（等 G2） |
| **配置归属** | 位流内**无**明文 FMC150 CDCE 高辨识度写表；作者仓无上位机（G1-a 公开结案）；EEPROM / RTL 拼帧 / 主机下发三种仍可能 |
| **FPGA 算法** | **不能命名**。先验偏向「深缓冲段采 + 时域上传」（形态 A），未实验证实 |

总目标（P1 ≥80% ✅、P2 每条 ≥🔶 + 排除表）——**未达成**。

---

## 2. 硬件与信号链（G0）

详见：`01_hardware/信号链_ADS62P49_DAC3283_CDCE72010.md`、`BOM_证据分级.md`、`时钟树.md`。

- FPGA：XC7K160T，IDCODE `0x0364C093`，未加密 Master SPI  
- 前端：U1 ADS62P49、U3 DAC3283、CDCE72010 + 491.52 MHz VCXO  
- 数字后端差异：FT600 USB3 + RTL8211 + DDR3×2（相对 FMC150 子卡）

命题基线表：`06_docs/G0_命题基线证据表.md`（✅0 / 🔶4 / ❓7）。

---

## 3. 资料对照与假说（G1）

- `03_architecture/算法线索_资料对照.md`：三种公开算法形态 A/B/C → 假说 H1–H8  
- `02_firmware/位流SPI常量搜索.md`：CDCE Reg0/A/B 等全 0 命中、无成簇  

---

## 4. 时钟与 SPI 实测（G2）——待填

回填：`05_tests/G2_时钟与SPI记录.md`。  
工具：`scripts/decode_spi_capture.py`。

### 4.1 时钟

（空）

### 4.2 SPI 寄存器与模式

（空）

### 4.3 P1 等级更新

| 命题 | G0 | G2 后 |
|------|----|-------|
| P1.1 | 🔶 | |
| P1.2 | 🔶 | |
| P1.3 | ❓ | |
| P1.4 | ❓ | |
| P1.5 | ❓ | |

---

## 5. 主动链路与数据（G3）——待填

回填：`05_tests/G3_ADDA闭环记录.md`（尚未建则实验时新建）。

---

## 6. 算法判别（G4）——待填

矩阵：`05_tests/G3G4_算法判别矩阵.md`。

### 6.1 保留假设

（空）

### 6.2 排除表

（空——无板阶段不预写排除）

### 6.3 P2 等级更新

| 命题 | G0 | G4 后 |
|------|----|-------|
| P2.1 | 🔶 | |
| P2.2 | ❓ | |
| P2.3 | ❓ | |
| P2.4 | ❓ | |
| P2.5 | 🔶 | |
| P2.6 | ❓ | |

---

## 7. 结论边界

1. 静态资料已用尽能回答的「芯片是谁、接口族是什么、对照谁」。  
2. **采样率 / 插值 / LVDS 边沿 / SPI 模式** → 必须 G2。  
3. **NCO·FIR·FFT 等算法名** → 必须 G3/G4；MCS 字符串维度不可用。  
4. 本草稿随实验回填；未回填章节不得当作已完成结论引用。
