# 目标完成审计 L0–L3（证据核对版）

> 审计日期：2026-08-28  
> 方法：对每层「列出证明完成所需的权威证据 → 打开实际文件 / 运行脚本核对 → 判定」。  
> 判定口径：**已证明**（权威证据齐、可复现）/ **未完成**（关键证据缺失）/ **证据弱**（有结论但旁证不足或依赖实物）。  
> 范围：仅针对无实物阶段的 L0–L3 静态目标。L4/L5 实测项不在本次审计范围（其待办不计为 L0–L3 缺口）。

## 总结论

| 层级 | 目标 | 判定 |
|------|------|------|
| L0 | 资料归档与版本矩阵 | **已证明** |
| L1 | 证据分级 BOM / 接口 / 空焊盘 | **已证明**（主芯片识别 ≥92%，达 ≥90% 成功标准） |
| L2 | 固件结构分析与位流提取 | **已证明**（脚本实跑复核通过） |
| L3 | 系统架构 / 时钟树 / 电源树 | **已证明**（静态口径） |

**L0–L3 四层均判定为「已证明」。** 残留的 🔶/❓ 项全部属于需要实物或微距补拍的收敛动作（归属 L4/L5 或补拍），不构成 L0–L3 的完成缺口。详见文末「非阻塞残留」。

---

## 关键脚本实跑核对（必做项）

命令：`python3 scripts/analyze_mcs.py`（读默认 `assets/firmware/20230825_s2056.mcs`），实际执行输出核对：

| 要求确认项 | 实跑输出 | 结果 |
|------------|----------|------|
| `XC7K160T` | `@0x90  IDCODE=0x0364C093  XC7K160T` | ✅ 命中 |
| `IDCODE 0x0364C093` | 同上，位流偏移 0x90 处精确匹配 | ✅ 命中 |
| `DEC=0` | CTL0 检查 8 处全部 `DEC=0(未加密)` | ✅ 命中 |

其它一致性输出：数据字节总量 6,692,572 B（6.383 MiB）、地址范围 `0x0..0x661EDC`、**地址间隙 0**、同步字 `AA995566` 仅 **1 处**（单镜像）、boot header `BB11220044` @0x23、首个 sync 后 1MiB 熵 ≈1.41 bit/byte。三项硬性标记全部命中。

---

## assets 在库核对

| 资产 | 在库情况 | 结论 |
|------|----------|------|
| 固件 `20230825_s2056.mcs` | git 已跟踪（18,824,626 B，md5 `1a6a7944954fd348a4f943f8c6daf62d`） | ✅ 在库（源真值） |
| 固件 `20230825_s2056.bin` | 本地存在（6,692,588 B，md5 `477a8469d7ff87c7e531b1d4a7106ae8`），按 `.gitignore` 规则不入库（可由 .mcs 再生） | ✅ 设计如此 |
| PCB 照片 | 7 张微信原图（图25–31）+ `S6_背正面配准_回投验证.jpg`，共 8 个 jpg 全部 git 已跟踪 | ✅ 在库 |

照片与 `assets/images/README.md` 索引一一对应；固件 .mcs 为源真值入库，.bin 为可再生派生物按约定 gitignore，均符合归档设计。

---

## L0 资料归档

**证明完成所需的权威证据**
1. 原始固件（.mcs）已归档且可校验。
2. PCB 照片全部归档并建立图号索引。
3. 版本/型号线索矩阵成表并标注证据等级。
4. 立项说明、课题范围边界（做/不做）明确。

**核对（实际文件）**
- `assets/firmware/20230825_s2056.mcs` git 已跟踪；`assets/images/*` 7 原图+S6 已跟踪。
- `assets/images/README.md`：图25–31 逐图内容要点 + S6 配准回投，索引完整。
- `00_meta/版本矩阵.md`：固件日 2023-08-25、代号 s2056、硬件贴纸 H8.2.3、批次 C11、贴标 2026/06、目标 FPGA=XC7K160T（IDCODE `0x0364C093`）、destly 关联，均带证据等级；Q05/Q06/Q07 已关闭。
- `00_meta/立项说明.md`：总目标、范围（做/不做）、成功标准 5 条齐备。

**判定：已证明。** 归档、索引、版本矩阵、立项/范围文档齐全且可校验。

---

## L1 证据分级 BOM

**证明完成所需的权威证据**
1. 带证据等级（A/B/C/D）的 BOM，主芯片识别率 ≥90%（成功标准第 1 条）。
2. 每条含丝印原文抄录 + 官方 device marking / 外围电路旁证。
3. 初稿争议项（USB 桥、ADC）有裁决记录；FPGA 型号与位流侧交叉验证。
4. 接口、空焊盘、DIP 等辅表齐备。

**核对（实际文件）**
- `01_hardware/BOM_证据分级.md`（第 2 版）：主芯片全表 U9 FPGA / U2 FT600Q-B / U1 ADS62P49 / U3 DAC3283 / U27 CDCE72010 / U14·U15 DDR3 / U37 RTL8211F / U21 FL128 Flash，加时钟树、电源、接口、小逻辑分表；等级定义 A/B/C/D 明确。
- 争议裁决记录：USB=FT600Q-B ✅关闭、ADC=ADS62P49IRGC ✅关闭、FPGA IDCODE 位流侧以 `0x0364C093` 关闭（与 L2 实跑一致）。
- 自评主芯片识别 **≥92%**，超过 ≥90% 成功标准。
- 接口定义表、空焊盘清单、DIP 开关分析等辅助文件均在库。

**判定：已证明。** L1 目标（≥90% + 证据分级 BOM）已达标，且核心器件识别与 L2 位流证据自洽。

---

## L2 固件结构分析

**证明完成所需的权威证据**
1. Intel-HEX/MCS 结构解析（记录统计、镜像重建、地址连续性）。
2. 目标器件 IDCODE = XC7K160T `0x0364C093`（脚本可复现）。
3. 加密状态判定（CTL0 DEC 位）。
4. 单/多镜像判定；备份/烧录/回滚 SOP。
5. 明文/软核/网络常量扫描结论（E1）。

**核对（实际运行 + 文件）**
- 实跑 `scripts/analyze_mcs.py`：IDCODE `0x0364C093`=XC7K160T、CTL0 全 DEC=0、单 sync、间隙 0、镜像 6,692,572 B、熵 1.41（见上「关键脚本实跑核对」）。
- `02_firmware/固件结构分析.md`：结论表与实跑输出一一吻合；明确纠正旧笔记 `0x03631093`（实为 Artix-7 家族码）。
- `02_firmware/位流网络常量搜索.md`：IP/MAC/端口命中全部判为零区/帧位巧合，整体熵 1.136、`0x00` 占 88.94% → 未加密原始配置位流，非带网络栈 CPU 固件（E1 软核阴性）。
- `02_firmware/SOP_kintex7_spi_flash_backup_program_rollback.md`：备份/烧录/回滚 SOP 在库。

**判定：已证明。** 位流结构、器件、加密状态、单镜像、软核阴性均以可复现脚本证实；SOP 齐备。

---

## L3 系统架构

**证明完成所需的权威证据**
1. 系统总体框图覆盖全部已确认器件/接口，每条连线带证据等级。
2. 数据链路（采集方向）逐级带宽预算 + 瓶颈结论。
3. 控制链路（JTAG / Flash 自举 / 运行时命令 / SPI 从机）分层。
4. 应用域假设 + 判别实验矩阵（含零成本静态项）。
5. 时钟树专项、电源树专项（静态口径「带 ❓ 的完整表格」）。

**核对（实际文件）**
- `03_architecture/L3_系统架构.md`：总体框图（mermaid）、数据链路带宽预算表（ADC 产出 vs USB3/GbE 瓶颈→DDR3 段采集推论）、控制链路三层 + SPI 从机表、H1/H2/H3 三假设 + E1–E7 判别矩阵，均在文。
- `01_hardware/时钟树.md`（v2）：X1 491.52M / X2 100M / X3 200M 时钟源表、CDCE72010 分配、mermaid 时钟树、待证实验 6 条 —— 满足 A4 静态出口。
- `01_hardware/电源树.md`（修订二）：输入链、U7/U8 TPS53515 ✅、U4 TPS54620 🔶、U10/U11 TPS74401 ✅、U12/U13/U16 QFN-16 类候选、轨假设与两级链路 —— 满足 A5 静态出口。

> 说明：`L3_系统架构.md` §5 验收表把 A4/A5 标为「未开始」，实为该文成稿早于时钟树/电源树专项定稿的**陈旧标注**；两份专项文件已实际完成静态口径。这是文档内一处需回填的措辞不一致（非层级缺口）。

**判定：已证明（静态口径）。** A1–A3、A6、A7 达标，A4/A5 已以「带 ❓ 完整表格」形态建立，满足 L3 出口条件。

---

## 非阻塞残留（均需实物 / 微距补拍，归属 L4/L5 或补拍，非 L0–L3 缺口）

1. RTL8211F 后缀（I/S）近拍定级；VCXO(X1) 厂家完整料号。
2. U4 首字符落实（TPS54620 确认）；U12/U13/U16 型号（需 ≥250 px/mm 微距 + 低角度侧光，或上电测 L18/L19/L20 轨）。
3. LDO 反馈分压 R 值（0402 无字）→ 实测定轨。
4. FPGA IDCODE 的 JTAG 读回交叉验证（位流侧已 ✅）。
5. USB/以太网通路实测、ADC/DAC 一次实测（L4/L5 方案+脚本已备，待板卡）。
6. 文档回填：`L3_系统架构.md` §5 中 A4/A5 状态由「未开始」更新为「已完成（静态）」。
