# 算法与 ADC/DAC 实现 · 研究计划与目标体系

> 目的：回答两件事——**用了什么算法**、**ADC/DAC 如何实现**。  
> 范围：只做 `20230825_s2056` / zero0000。不做 4431。  
> 现实约束：现有 mcs+照片**已到静态上限**；要升到「可答辩的实现级结论」，必须分阶段引入新证据（资料 → 被动电气 → 主动激励 → 可选深度逆向）。

---

## 0. 问题拆解（先把「想知道」写成可验收命题）

### P1 — ADC/DAC「如何实现」（硬件+接口+工作模式）

| 命题 ID | 想确认的内容 | 最低成功标准 |
|---------|--------------|--------------|
| P1.1 | 模拟前端拓扑 | SMA→耦合→ADS62P49 / DAC3283→SMA 与照片一致；通道对应 SIG1/2、DAC1/2 |
| P1.2 | 数字接口 | ADC→FPGA 为 DDR LVDS；FPGA→DAC 为 8-bit DDR LVDS+FRAME（示波器或 ILA 见到边沿/帧） |
| P1.3 | 采样/更新时钟 | 测得 ADC 样钟、DAC DACCLK 实际频率（候选 245.76 / 491.52 MHz 族） |
| P1.4 | 器件工作模式 | SPI 抓到的寄存器：ADC DDR/增益；DAC 插值倍数、粗混频开关；CDCE 分频与 LOCK |
| P1.5 | 配置归属 | 上电后配置来自 Flash 位流内逻辑自动 SPI，还是主机事后下发 |

### P2 — 「用了什么算法」（FPGA 数据通路）

| 命题 ID | 想确认的内容 | 最低成功标准 |
|---------|--------------|--------------|
| P2.1 | 有无深缓冲 | DDR3 读写活动与段采/连续流行为可区分 |
| P2.2 | 有无抽取/重采样 | 主机侧样率 ≪ ADC 时钟，或寄存器/命令暴露抽取得数 |
| P2.3 | 有无数字下变频/NCO/FIR | 频谱平移、镜像抑制、滤波响应与激励频率的关系可复现 |
| P2.4 | 有无 FFT/检波等 | 上传数据已是谱/功率而非时域 IQ/实采样 |
| P2.5 | 有无软核调度 | ILA/总线看到 CPU 总线周期，或仅纯流水线 |
| P2.6 | 主机协议语义 | 命令→采/放/触发的状态机可画出 |

**证据等级**：每条结论必须标 ✅ / 🔶 / ❓，禁止无证据点名「一定是 FFT」。

---

## 1. 总目标（Goal）与阶段目标（G0–G4）

### 总目标

> 在可复现实验与文档约束下，把 P1 提升到 **≥80% 命题 ✅**，把 P2 提升到 **每条至少 🔶 并排除明显不可能项**；输出一份《ADC_DAC实现与算法推断报告》。

### 阶段目标（必须按序，不可跳 G2 做 G3 的「算法定性」）

| 阶段 | 名称 | 核心产出 | 通过门禁（Gate） | 状态（2026-08-28） |
|------|------|----------|------------------|-------------------|
| **G0** | 基线固化 | 器件 BOM、信号链假说、位流身份、SPI 靶标表、**命题基线表** | 现有档案可引用；本阶段不依赖板 | ✅ **通过**（见 `G0_命题基线证据表.md`） |
| **G1** | 外部资料补强 | 同类 HDL/手册对照 + 可检验假说 | 至少多 1 份非照片证据，或书面接受「无资料」 | ✅ **有增量通过**（对照 + SPI 散点 + **KC705_DDS MIF 整表阴性**）；本板专属上位机仍缺 |
| **G2** | 被动电气确认 | 时钟实测表 + SPI 初始化日志 | P1.3、P1.4 至少各 1 条 ✅ | ⬜ 预备齐（仪器清单 + 记录模板 + `decode_spi_capture.py`）；**等板** |
| **G3** | 主动链路确认 | ADC 单音进、DAC 单音出、USB/ETH 收数 | P1.1–P1.2 ✅；P2.1 或 P2.2 达 🔶 | ⬜ 方案见 `G3G4_算法判别矩阵.md`；等 G2 |
| **G4** | 算法判别矩阵 | 算法假设打分表 + 排除项 | P2.3–P2.6 每条有实验行；报告定稿 | ⬜ 矩阵+报告骨架已建；等 G3 |

```text
G0 现有资料 ──► G1 资料/软件 ──► G2 被动测(钟/SPI) ──► G3 主动测(AD/DA/流) ──► G4 算法判别
     ↑ 你当前可停在此              ↑ 不要板也能做一部分         ↑ 开始需要板与仪器
```

---

## 2. 各阶段怎么做（方法 + 工具 + 记录）

### G0 — 基线（现状：✅ 可视为完成）

**已有**：`信号链_*.md`、`FMC150_SPI靶标检查表.md`、`位流特征与软核线索.md`、`L3_系统架构.md`。  

**本阶段动作（可选收尾）**：
- [ ] 把「已确认 vs 假说」两列表单独贴进本计划附录（避免和算法结论混谈）
- [ ] 明确：**G0 不能回答 P2 算法名**

### G1 — 不靠板也能推进的计划（优先）

目标：用**非位流**资料缩小算法搜索空间。

| 动作 | 做法 | 成功标准 |
|------|------|----------|
| G1-a | 索要/检索上位机、DLL、示例工程 | 出现函数名/寄存器头文件/采样格式说明 |
| G1-b | 对照公开 FMC150 / ADS62P49 EVM / DAC3283 EVM HDL | 列出「同构点 / 差异点」表（本板有 FT600+ETH+DDR，FMC150 无此整机） |
| G1-c | 若有第二版固件 mcs | 差分位流占用/字符串（仍可能无明文，但可看体积差） |
| G1-d | 协议侧：若作者提供抓包样例（哪怕别人抓的） | 先做离线解析，再谈复现 |

**G1 退出**：要么拿到可读协议/HDL 线索，要么记录「G1 无增量，进入 G2」。

### G2 — 被动确认 ADC/DAC「实现」（最小仪器）

> 需要板卡，但**不必**先会主机协议。重点答 **P1**。

| 步骤 | 内容 | 工具 | 对应命题 |
|------|------|------|----------|
| G2-1 | 按 Day-1：安全上电 + Flash 只读备份 | 电源、JTAG、SOP | 安全前提 |
| G2-2 | 探 CDCE 输出脚 / ADC CLKP / DAC DACCLK | 示波器（≥1 GHz 更佳，500 MHz 可看分频后） | P1.3 |
| G2-3 | 逻辑分析仪挂 SPI（SCLK/MOSI/CSn×3） | LA ≥50 MHz | P1.4、P1.5 |
| G2-4 | 对照靶标表勾选：ADC 0x41 DDR、DAC 插值域、CDCE LOCK | 文档勾选 | P1.4 ✅ |
| G2-5 | （可选）探 ADC LVDS 随路时钟、DAC FRAME | 差分探头/LA | P1.2 🔶→✅ |

**G2 退出门禁**：写出《时钟实测表》+《SPI 初始化译码表》；P1.3、P1.4 达标。

### G3 — 主动确认数据通路（答 P1 剩余 + P2.1/2.2）

| 步骤 | 内容 | 对应命题 |
|------|------|----------|
| G3-1 | 打通 USB 或 ETH 一条收数路径（L4） | 主机通路 |
| G3-2 | SIG 注入已知正弦 → 落盘 → `l5_fft_check` / 自写解析 | P1.1、P2.4（若已是谱则提前暴露） |
| G3-3 | 改注入频率/幅度，看上传数据如何变 | P2.2 / P2.3 |
| G3-4 | DAC 路径：下发或板内激励 → 示波器看 DAC SMA | P1.1、P1.4（插值表现） |
| G3-5 | 短时连续采 vs 触发采，观察 DDR/吞吐 | P2.1 |

**G3 退出门禁**：至少一条「激励→数据→可解释频谱/时域」闭环；记录原始文件哈希。

### G4 — 算法判别矩阵（答「用了什么算法」）

对每个假设打分（0–2：无证据 / 弱 / 强），并设计**可证伪实验**：

| 假设 | 预期现象 | 证伪现象 |
|------|----------|----------|
| H-纯时域打包上传 | 改 Fin，数字谱峰跟着线性走；无固定本振差频 | 峰固定在某 IF 或已是幅度谱 |
| H-DDC/NCO | 存在可调 LO；镜像/边带可调 | 完全无复基带结构 |
| H-FIR/半带抽取 | 样率阶跃（÷2/÷4）；通带滚降 | 样率=时钟且无滚降 |
| H-板内 FFT | 上传长度固定 2^n 点谱；相位信息弱/无 | 连续时域流、可变长度帧 |
| H-PRBS/自检 | 无输入仍有确定性码型 | 无输入接近噪声底 |
| H-软核主控 | SPI/USB 命令延迟呈现软件抖动；BRAM/总线活跃 | 纯固定流水延迟 |

**G4 退出门禁**：矩阵填完；每个 H 标注保留/排除；总报告增加「算法结论」节（允许最终仍是 🔶 多假设并存，但必须写清证据）。

---

## 3. 资源与依赖（规划时就要写清）

| 层级 | 最低资源 | 没有时的替代 |
|------|----------|--------------|
| G1 | 作者联系 / 闲置软件包 / 公开 HDL | 跳过，直接 G2 |
| G2 | 板、限流电源、示波器、LA、JTAG | **无法可靠回答 P1.3/P1.4** |
| G3 | + USB3 主机或网口、信号源、SMA | 仅能停在时钟/SPI |
| G4 | + 可脚本化收数、可选 ILA 许可 | 算法停在 🔶 假说层 |

安全红线：一律先走 `Day1_实物执行手册.md`（备份前不写 Flash、不盲接高压）。

---

## 4. 交付物清单（每阶段必须入库）

| 阶段 | 仓库路径（建议） |
|------|------------------|
| G0 | 已有 `01_hardware/信号链_*.md` 等 |
| G1 | `03_architecture/算法线索_资料对照.md`；加深 `02_firmware/位流SPI常量搜索.md` |
| G2 | `05_tests/G2_时钟与SPI记录.md`（模板已建）+ LA 原始导出（大文件可外链） |
| G3 | `05_tests/G3_ADDA闭环记录.md` + samples 哈希；方案挂 `G3G4_算法判别矩阵.md` |
| G4 | `06_docs/ADC_DAC实现与算法推断报告.md`（骨架已建，待实测回填） |

推送：仍在分支 `cursor/zero0000-research-7055`（或其后继研究分支）持续 commit。

---

## 5. 成功标准（何时算「实现了你想知道的」）

**最低合格（Must）**
1. P1.1–P1.4 均有 ✅ 或强 🔶（接口+时钟+SPI 模式说清）  
2. P2 至少完成「排除表」：明确哪些算法**不像**  
3. 报告可被第三人按记录复现关键实验  

**完整合格（Should）**
4. P2.1–P2.4 中至少 2 条升到 ✅  
5. 主机命令/数据帧格式有初步字典  

**卓越（Could）**
6. 关键数据通路框图细化到模块级（缓冲→处理→打包→FT600/ETH）  
7. 与 FMC150 参考设计的逐模块异同表  

---

## 6. 与当前口径的关系

- 若继续「**先不考虑实体板**」：只执行 **G0 收尾 + G1**；G2–G4 登记为计划，不空转。  
- 若目标是「**一定要知道算法**」：必须接受 **G2 起需要板与仪器**；纯静态不可能完成 P2。  
- 抓包：放在 **G2（SPI）/ G3（USB·ETH）**，不是第一步。

---

## 7. 建议的近期执行顺序（可勾选）

### 现在（无板）
- [x] 确认本计划作为正式目标文档（本文）  
- [x] G0：命题基线证据表（`G0_命题基线证据表.md`）  
- [x] G1：FMC150/公开实现对照 + 假说表（`算法线索_资料对照.md`）  
- [x] G1 加深：位流 SPI 常量搜索（`位流SPI常量搜索.md`，高辨识度 CDCE 字 0 命中）  
- [x] G1 再加深：TSW4200 对照 + 上位机公开侧结案（`G1_TSW4200与上位机检索.md`）  
- [x] G2 预备：仪器清单 + 记录模板 + `decode_spi_capture.py` + `g2_mode_infer.py`  
- [x] G3/G4 预备：判别矩阵 + `G3_ADDA闭环记录.md` + `g3_tone_analyze.py` + 报告骨架  
- [x] G1 再加深：KC705_DDS 真实 MIF 整表阴性（`KC705_DDS_SPI_MIF对照.md`）  
- [x] G2 预备加深：照片探针地图 + SW5/SW42 出厂拨位存档（`G2_照片探针地图.md`）  
- [x] G2 资源门禁说明（`G2_资源门禁_用户动作.md`）——等用户提供钟/SPI 数据或工位  
- [x] G2 预备加深：SPI CSV `--auto-map` + `g2_inbox/examples` 合成演练（ingest 忽略 example）  
- [x] G1/P2.5：确认公开 prjxray **无** xc7k160t（`prjxray_K160T帧图可得性.md`）  
- [x] G2 摩擦降低：`G2_投放三步.md`  
- [x] Must-2 归因陷阱排除：`G3G4` §4.1b（DAC 片内 FIR/混频 ≠ FPGA 算法）  
- [x] Must-2 §4.2：通路形态排除表已填（硬 IP 本体仍开放；见矩阵 §4.2）  
- [x] G2 管线：单侧钟→强 🔶；ingest 产出 `G2_G0回填提案.md`  
- [x] G1 再加深：Conserviss/FMC150-VC707 硬件验证表（钟计划 B + SPI 全表；`G1_FMC150_VC707对照.md`）  
- [x] 静态结论卡：`当前结论卡_①②.md`；SPI checklist 剖面评分（Conserviss/E2E）  
- [x] G1 再加深：Conserviss limitations R11（带宽/2:1/iotest；DAC pre-sync 全表评分）  
- [x] G1/G2 预备：`decode_cdce_profile.py`（Table-8→Hz）+ infer SPI→计划 B 先验  
- [x] G2 管线：`ingest --demo` 端到端（Conserviss-min+计划B；禁止回填 G0）  
- [x] Must 机器核验：`scripts/audit_must.py`（未达标 exit 1）  
- [x] G1 再加深：R5b Wiley/RHINO 实测钟族（61.44 / 163.84÷3）写入 `算法线索` + `g2_mode_infer` 族表  
- [x] G1 再加深：R11c Conserviss 共享 IDELAY / 无 bitslip → 修正 H5 证伪口径  
- [x] G1/G2 预备：R11d SPI 单向（独立 SDO）陷阱 + G2 清单 C3 双先验（A/B）  
- [ ] G1 加分：若获得**私发**上位机/原理图再增量

### 有板后（最短路径到「实现级」）
- [ ] Day-1 备份与安全上电  
- [ ] G2：时钟 + SPI（优先于业务抓包）  
- [ ] G3：ADC 单音一条闭环  
- [ ] G4：填算法判别矩阵，出报告  

---

## 8. 一句话

**计划主轴：先钉死 ADC/DAC 的时钟与 SPI 工作模式（实现），再通过可控激励看数据如何变（算法）。**  
现有资料只覆盖 G0；要满足你「想知道算法与实现」的目标，请按 G1→G4 推进，并为 G2+ 准备板卡与示波器/逻辑分析仪。
