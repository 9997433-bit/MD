# G2 资源门禁（用户动作 · 解锁 Must）

> 日期：2026-08-28  
> 范围：仅 `20230825_s2056`。不做 4431 / destly。  
> 目的：总目标 Must（P1.1–P1.4 达 ✅/强 🔶）**原理上需要 G2 实测**；本云环境无板、无 USB 设备、作者仓无上位机。

---

## 1. 当前证据天花板（不可空转抬高）

| 命题 | 级 | 无板能否再升 |
|------|----|--------------|
| P1.1 模拟拓扑 | 🔶 | 仅微距照片可能微升 |
| P1.2 DDR LVDS 接口族 | 🔶 | ❌ datasheet 已到顶 |
| P1.3 采样/更新钟 | ❓ | ❌ **必须测频** |
| P1.4 SPI 工作模式 | ❓ | ❌ **必须抓 SPI** |
| P1.5 配置归属 | ❓ | ❌ 必须上电嗅探 |
| P2.* 算法模块名 | ❓/🔶 | 无主机资料则须 G3/G4 |

静态已完成：G0/G1、MIF/SPI 阴性、BRAM、探针地图、脚号蜂鸣表、G2/G3 脚本与模板。

---

## 2. 请你提供（二选一即可解锁）

### A. 上传 G2 原始数据（推荐，不需把板接到云端）

按 `05_tests/G2_当日执行包.md` + `G2_照片探针地图.md`：

1. Day-1：限流上电 + Flash 只读备份（`Day1_实物执行手册.md`）  
2. 钟：至少 **C2（ADC CLKP）或 C3（DAC DACCLK）** 测得 Hz → 填 `g2_clocks.json`  
3. SPI：断电蜂鸣 R83/R88/R89/R105/R106 → 脚号表 §3.5；焊飞线；**仅上电无主机**抓 ≥5 s CSV  
4. 运行：

```bash
cd zero0000-research
# 列名可用默认或 Saleae 别名；--auto-map 自动解析
python3 scripts/decode_spi_capture.py capture.csv --auto-map --json g2_spi_decode.json
python3 scripts/g2_mode_infer.py --clocks g2_clocks.json --spi g2_spi_decode.json
```

5. 把文件放进 `05_tests/g2_inbox/`（**根目录**，不要放进 `examples/`）：

| 文件 | 内容 |
|------|------|
| `g2_clocks.json` | 由 `g2_clocks.template.json` 复制并填**实测** Hz（全 null 会被拒） |
| `spi_capture.csv` | LA 导出 |

然后：

```bash
cd zero0000-research
python3 scripts/ingest_g2_inbox.py
```

产出 `05_tests/G2_inbox_infer_report.md`（含 sha256）。Agent / 你复核后回填 G0。  
合成 CSV 仅作列名演练：`05_tests/g2_inbox/examples/`（ingest **不会**当实测吞掉）。

### B. 提供可访问的上电工位

板 + ≥1 GHz 示波（或计数器）+ ≥8ch LA + JTAG；告知访问方式。Agent 按 Day-1→G2 执行。

### C. 可选加速（不替代 A/B）

上位机 / 原理图 / 第二版 mcs → 仅助 P2.2/2.3/2.6 的 ❓→🔶。

---

## 3. 明确不需要你做的

- 再搜公开 ROM/MIF（已阴性到顶）  
- 在无 A/B 时要求 Agent「继续分析位流得出采样率」——做不到  

---

## 4. 复原拨位基线

SW5 = **OFF/OFF**；SW42 = **仅位5 ON**（`G2_照片探针地图.md` §4）。实验后复原。
